apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name:postgres-operator-user
subjects:
  - kind: ServiceAccount
    name: "{{ item.name }}"
    namespace: "{{ item.namespace | default('default', true) }}"
roleRef:
  kind: ClusterRole
  name: secret-reader
  apiGroup: rbac.authorization.k8s.io